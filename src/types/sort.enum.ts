export enum Order {
  ASC = "asc",
  DESC = "desc",
}

export enum Sortbycolumn {
  NAME = "name",
  CREATEDTIME = "createdTime",
  UPDATEDTIME = "updatedTime",
}

export enum Searchbycolumn {
  NAME = "name",
  TITLE = "title",
  TYPES = "types",
  DIVISION = "division",
  USERNAME = "username",
}
